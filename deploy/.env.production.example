# Salin ke .env di VPS (JANGAN commit file .env asli ke git).
# Generate JWT_SECRET acak dengan: openssl rand -base64 48

DATABASE_URL="file:./prisma/dev.db"
JWT_SECRET="GANTI_DENGAN_HASIL_openssl_rand_-base64_48"
NODE_ENV="production"
